{
  "dependencies": {
    "react-mic": "^12.4.6",
    "server": "file:.."
  },
  "scripts": {
    "remove-nodemodules": "rimraf node_modules && npm run remove-nodemodules --prefix voice-recorder"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
